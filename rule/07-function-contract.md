# Function Contract

- One clear purpose per function
- Inputs and outputs must be explicit
- Side effects must be obvious
- Business failure uses `Result` or typed outcome
- Preconditions belong at the owning boundary
- Postconditions must leave valid state
- Avoid hidden global writes
