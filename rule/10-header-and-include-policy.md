# Header And Include Policy

- Headers include only what they need
- Prefer forward declarations when enough
- Put heavy includes in `.cpp` when possible
- Never use `using namespace` in headers
- Keep headers self-contained
