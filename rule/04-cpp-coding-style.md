# C++ Coding Style

- Standard: C++20
- Use `enum class`
- Use `constexpr` for compile-time constants
- Use `std::optional` for optional values
- Use `std::string_view` only when lifetime is safe
- Prefer standard containers and algorithms
- Avoid macros unless integration requires them
- Avoid C-style casts
- Never use `double` for money
